module.exports = function(array) {
    var counts = {};

    for (let i = 0; i < array.length; i++) {
        let key = array[i];
        counts[key] = (counts[key]) ? counts[key] + 1 : 1;
    }

    return counts;
};
