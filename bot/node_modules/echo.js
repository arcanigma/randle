const CONFIG = require('config');

module.exports = function(controller) {

    controller.hears( /^!echo\b(.*)/, CONFIG.HEAR_TYPES, function(bot, message) {
        bot.reply(message, match[1]);
    });

};
