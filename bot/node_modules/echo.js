const CONFIG = require('config');

module.exports = function(controller) {

    controller.hears( /^!echo\b(.*)/, CONFIG.HEAR_ALL, function(bot, message) {
        bot.replyWithTyping(message, match[1]);
    });

};
